# Supervisor Agent Lambda tools for Connect Analytics Platform
#
# Tools:
#   get_queue_health       — Queue health metrics (size, wait, SLA, handle time)
#   get_abandonment_analysis — Abandonment rate and patterns
#   get_agent_utilization  — Per-agent occupancy and status
#   trigger_sla_alert      — Publish SLA breach events to EventBridge
